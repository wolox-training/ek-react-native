import { StyleSheet } from 'react-native';

const shadowSize = 40;

export default StyleSheet.create({
  shadow: {
    alignItems: 'center',
    borderRadius: 30,
    elevation: 3,
    height: shadowSize,
    justifyContent: 'center',
    width: shadowSize
  }
});
